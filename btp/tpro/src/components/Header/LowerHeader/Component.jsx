import styeld from "styled-components";

const LowerHeaderComponent = ({}) => {
  return <LowerHeaderLine>로어에요</LowerHeaderLine>;
};

const LowerHeaderLine = styeld.div``;

export default LowerHeaderComponent;
