import styeld from "styled-components";

const TopHeaderComponent = ({}) => {
  return <TopHeaderLine>탑이에여</TopHeaderLine>;
};

const TopHeaderLine = styled.div``;

export default TopHeaderComponent;
